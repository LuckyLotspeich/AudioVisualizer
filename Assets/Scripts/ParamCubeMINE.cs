using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ParamCubeMINE : MonoBehaviour
{
    // public int band;
    // public float startScale = 1f;
    // public float scaleMultiplier = 10f;
    // public GameObject cube;
    // void Start()
    // {
    //     // Spawn cubes from Bands in AudioInputManager
    //     for (int i = 0; i < AudioInputManager.numBands; i++)
    //     {
    //         float rotationAngle = 360f / AudioInputManager.numBands * i;

    //         GameObject instanceCube = (GameObject)Instantiate(cube);
    //         instanceCube.transform.parent = transform; // Set the cube as child of this object
    //         instanceCube.transform.localPosition = Vector3.forward * 100; // Adjust position if needed
    //         instanceCube.transform.localRotation = Quaternion.Euler(0, rotationAngle, 0); // Apply rotation

    //         // Add ParamCube component to the cube and assign band
    //         ParamCube paramCube = instanceCube.AddComponent<ParamCube>();
    //         paramCube.band = i;
    //     }
    // }

    // void Update()
    // {
    //     transform.localScale = new Vector3(transform.localScale.x, (AudioInputManager.freqBand[band] * scaleMultiplier) + startScale, transform.localScale.z); 


    // }
}


        // transform.localScale = new Vector3(transform.localScale.x, (AudioInputManager.freqBand[band] * scaleMultiplier) + startScale, transform.localScale.z); 
